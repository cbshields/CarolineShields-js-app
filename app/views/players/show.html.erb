<h1><%=@player.name%></h1>

Age: <%= @player.age %><br>
Address: <%= @player.address %>

<h2>Sports</h2>


<% @player.sports.each do |sport| %>
     <!-- <li><%= link_to sport.name, sport_path(sport)%></li> -->

     <% sport.positions.each do |position| %>
       <li><%= link_to "#{sport.name} - #{position.name}", sport_path(sport)%></li>

    <% end %>
 <% end %><br><br>

<%= link_to "Edit Player", edit_player_url%> &nbsp &nbsp<%= link_to "Delete Player", @player, method: :delete, data: { confirm: "Are you sure you want to delete this player?" } %> &nbsp &nbsp<%=link_to "Go To Players", players_path %> &nbsp &nbsp <%=link_to "Go To Sports", sports_path %><br><br>
