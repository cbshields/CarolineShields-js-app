<h1>Players</h1>

<% @players.each do |player| %>

  <li><%= link_to player.name, player_sports_path(player), :class => "load_sports" %></li>
  <div class="sport_list">
  <%#= render 'comments/comments' %>
</div>
<% end %>
<h1>Under 18</h1>
<% @under_18.each do |player| %>
  <li><%= link_to player.name, player_positions_path(player) %></li>
<% end %>

<h1>Over 18</h1>
<% @over_18.each do |player| %>
  <li><%= link_to player.name, player_positions_path(player) %></li>
<% end %><br><br>

<%=link_to "Go To Sports", sports_path %><br><br>
